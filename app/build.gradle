plugins {
    id 'java'
}

dependencies {
    implementation project(':lib')

    testImplementation(testFixtures(project(':lib'))) // comment out to make main compile
}

test {
    useJUnitPlatform()
}
